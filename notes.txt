démarrer le conteneur:
docker compose up -d 
stopper le conteneur:
docker compose down
conection à la db:
docker exec -it db-cv psql -U samuel -d CvData
connecte le back:
docker ps
docker network connect cv-reseau (port du back)

docker network inspect cv-reseau



docker exec -u root -it CV-front apt update
docker exec -u root -it CV-front apt install -y curl
docker exec -it CV-front curl http://CV-back:8080/CV/Competence 


docker exec -i db-cv psql -U samuel -d CvData < init.sql

Get-Content .\init.sql | docker exec -i db-cv psql -U samuel -d CvData